var AppShowcase = function() {
  function u() {
    n.on("click", a);
    r.on("click", function() {
      f($(this));
      return !1;
    });
  }
  function a() {
    e.addClass("ac-gridview");
    o.off("click").on("click", function() {
      f();
    });
    return !1;
  }
  function f(t) {
    e.removeClass("ac-gridview");
    if (t) {
      i.attr("src", t.find("img").attr("src"));
      s.text(t.find("span").text());
    }
  }
  var e = $("#ac-wrapper"), t = e.find(".ac-device"), n = t.children("a:first"), r = e.find(".ac-grid > a"), i = t.find("img"), s = t.find(".ac-title"), o = $("body");
  return {
    init: u
  };
}();